# ============================================================
# Cleantech Quant API — requirements.txt (v2.0 — updated)
# ============================================================

# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# --- Database ---
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# --- Cache / Queue ---
redis[asyncio]==5.0.4
celery[redis]==5.3.6

# --- Auth / Security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic[email]==2.7.1
pydantic-settings==2.2.1

# --- HTTP client (async) ---
httpx[http2]==0.27.0

# --- RSS feed parsing (NEW: for news scraping) ---
feedparser==6.0.11

# --- Data / math ---
numpy==1.26.4
pandas==2.2.2
scipy==1.13.0

# --- Reporting / exports ---
openpyxl==3.1.2        # working Excel financial models (live formulas)
reportlab==4.2.0       # automated weekly PDF briefings

# --- Auth (runtime imports use PyJWT + bcrypt directly) ---
PyJWT==2.8.0
bcrypt==4.1.3

# --- Payments ---
stripe==9.6.0

# --- Email ---
sendgrid==6.11.0

# --- AWS ---
boto3==1.34.107

# --- Monitoring ---
sentry-sdk[fastapi]==2.2.0

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
factory-boy==3.3.0

# ============================================================
# New environment variables required (add to .env):
#
# ENTSOE_API_KEY=<your ENTSO-E Transparency Platform token>
#   → Register free at: https://transparency.entsoe.eu/usrm/user/createPublicUser
#
# EIA_API_KEY=<your EIA Open Data API key>
#   → Register free at: https://www.eia.gov/opendata/register.php
#
# Note: PubMed, arXiv, DOE OSTI, GDELT, AEMO, H2IQ, IEA 
#       endpoints used are all free/public with no API key required.
# ============================================================
